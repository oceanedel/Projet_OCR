#include "word_processor.h"
#include <stdio.h>

// Placeholder for word recognition
int process_words(const char* words_dir, const char* output_file) {
    printf("\n[WORDS] Word processing not yet implemented\n");
    printf("[WORDS] Words dir: %s\n", words_dir);
    printf("[WORDS] Output: %s\n", output_file);
    
    // TODO: Implement word segmentation and recognition
    // For now, this is optional since solver can work with just grid
    
    return 0;
}
